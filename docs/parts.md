# Architectural parts

Generated by `preview/generate-parts-docs.cjs`; do not edit this file by hand.

Parts are bounded plan fragments. Their material defaults are semantic caller palette roles, so a plan must map each selected role to a block state. Cells emitted by a part remain owned by the component that calls it. Stair roles named `stair-up-south` must resolve to a bottom, south-facing stair. Their public `facing` parameter means the ascending direction; local straight flights rise toward +Z and part rotation supplies the other directions.

## `balcony.planted`

A shallow planted balcony with a continuous guard and planted outer edge.

- Style: `civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},3,{"param":"depth"}]` (max exclusive).
- Test call: `{"at":[6,5,6],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..8; default 4 |
| `depth` | integer, 2..4; default 2 |
| `floor` | palette role; default `floor` |
| `railing` | palette role; default `railing` |
| `planter` | palette role; default `planter` |
| `plant` | palette role; default `plant` |
| `facing` | enum (north, east, south, west); default north |

## `bed.alcove`

A compact bed alcove with a headboard wall and side clearances for a small apartment.

- Style: `interior-civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},3,{"param":"depth"}]` (max exclusive).
- Test call: `{"at":[6,5,6],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..6; default 3 |
| `depth` | integer, 3..5; default 3 |
| `floor` | palette role; default `floor` |
| `wall` | palette role; default `wall` |
| `bed` | palette role; default `bed` |
| `facing` | enum (north, east, south, west); default north |

## `core.lift`

A compact enclosed lift shaft with floor and roof closures.

- Style: `civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,-1,0]` to `[3,{"param":"height","offset":1},3]` (max exclusive).
- Test call: `{"at":[5,5,5],"params":{},"expect":{"valid":true}}`

| Parameter | Contract |
| --- | --- |
| `height` | integer, 3..16; default 6 |
| `wall` | palette role; default `wall` |
| `air` | palette role; default `air` |
| `floor` | palette role; default `floor` |

## `entrance.canopy`

A legible, supported entrance canopy with two clear-front columns.

- Style: `civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},3,{"param":"depth"}]` (max exclusive).
- Test call: `{"at":[5,5,5],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..8; default 4 |
| `depth` | integer, 2..5; default 3 |
| `roof` | palette role; default `trim` |
| `column` | palette role; default `column` |
| `facing` | enum (north, east, south, west); default north |

## `facade.pier-bay`

A framed facade bay with structural piers, recessed glazing and a guard rail.

- Style: `civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},{"param":"height"},2]` (max exclusive).
- Test call: `{"at":[6,5,6],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..8; default 4 |
| `height` | integer, 3..8; default 4 |
| `pier` | palette role; default `wall` |
| `glass` | palette role; default `glass` |
| `rail` | palette role; default `railing` |
| `facing` | enum (north, east, south, west); default north |

## `facade.punched-window`

A solid wall panel with a proportioned punched window and optional shade.

- Style: `old-town-compatible`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},{"param":"height"},2]` (max exclusive).
- Test call: `{"at":[6,5,6],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..8; default 4 |
| `height` | integer, 3..8; default 4 |
| `wall` | palette role; default `wall` |
| `glass` | palette role; default `glass` |
| `shade` | palette role; default `shade` |
| `awning` | enum (none, awning); default none |
| `facing` | enum (north, east, south, west); default north |

## `kitchen.run`

A linear kitchen run with base cabinets, a continuous counter and a tall appliance end.

- Style: `interior-civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},3,1]` (max exclusive).
- Test call: `{"at":[5,5,5],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..8; default 4 |
| `cabinet` | palette role; default `cabinet` |
| `counter` | palette role; default `counter` |
| `appliance` | palette role; default `appliance` |
| `facing` | enum (north, east, south, west); default north |

## `roof.parapet`

A bounded low roof parapet enclosing a rectangular roof zone.

- Style: `civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},{"param":"height"},{"param":"depth"}]` (max exclusive).
- Test call: `{"at":[4,5,4],"params":{},"expect":{"valid":true}}`

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..12; default 6 |
| `depth` | integer, 3..12; default 6 |
| `height` | integer, 1..3; default 1 |
| `coping` | palette role; default `trim` |

## `roof.screen`

A screened roof-services enclosure with solid end posts and a permeable face.

- Style: `civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},{"param":"height"},1]` (max exclusive).
- Test call: `{"at":[5,5,5],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 2..8; default 4 |
| `height` | integer, 2..5; default 3 |
| `post` | palette role; default `trim` |
| `screen` | palette role; default `screen` |
| `facing` | enum (north, east, south, west); default north |

## `seat.pair`

A paired fixed seat and shared table arrangement for lobby, balcony or public-room use.

- Style: `interior-civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,0]` to `[{"param":"width"},2,3]` (max exclusive).
- Test call: `{"at":[6,5,6],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..5; default 3 |
| `seat` | palette role; default `seat` |
| `table` | palette role; default `table` |
| `facing` | enum (north, east, south, west); default north |

## `sign.wall-framed`

A framed wall sign with a calm dash-rule fallback for civic wayfinding.

- Style: `civic-wayfinding`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,0,1]` to `[{"param":"width"},{"param":"height"},3]` (max exclusive).
- Test call: `{"at":[6,5,6],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (north=0, east=1, south=2, west=3).

| Parameter | Contract |
| --- | --- |
| `width` | integer, 3..8; default 4 |
| `height` | integer, 3..6; default 4 |
| `wall` | palette role; default `wall` |
| `trim` | palette role; default `trim` |
| `sign` | palette role; default `sign` |
| `facing` | enum (north, east, south, west); default north |

## `stair.straight`

A supported straight stair flight with a landing-length run.

- Style: `civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,-1,0]` to `[{"param":"width"},{"param":"rise"},{"param":"rise","offset":1}]` (max exclusive).
- Test call: `{"at":[4,5,4],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (south=0, west=1, north=2, east=3).

| Parameter | Contract |
| --- | --- |
| `rise` | integer, 2..8; default 4 |
| `width` | integer, 1..3; default 2 |
| `stair` | palette role; default `stair-up-south` |
| `support` | palette role; default `floor` |
| `facing` | enum (north, east, south, west); default south |

## `stair.switchback`

A compact two-flight stair wrapped around a shared intermediate landing; rise is per flight (total rise is twice this value).

- Style: `civic-modern`
- Provenance: Codex / gpt-5.6-terra — initial architectural vocabulary
- Local bounds: `[0,-1,0]` to `[{"param":"width","scale":2,"offset":1},{"param":"rise","scale":2},{"param":"rise","offset":1}]` (max exclusive).
- Test call: `{"at":[6,5,6],"params":{},"expect":{"valid":true}}`
- Rotation: `facing` (south=0, west=1, north=2, east=3).

| Parameter | Contract |
| --- | --- |
| `rise` | integer, 2..8; default 2 |
| `width` | integer, 2..3; default 2 |
| `stair` | palette role; default `stair-up-south` |
| `support` | palette role; default `floor` |
| `facing` | enum (north, east, south, west); default south |
